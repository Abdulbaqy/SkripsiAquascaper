<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiUserRealtimeFragment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MultiUserRealtimeFragment" parent="Base.Theme.MultiUserRealtimeFragment" />


    <!-- system background. -->
    <style name="SCREEN">
        <item name="android:background">
            #fff
        </item>
    </style>

    <!-- text background. -->
    <style name="TEXT">
        <item name="android:textColor">
            #fff
        </item>
    </style>

    <!-- button background. -->
    <style name="BUTTON">
        <item name="android:background">
            #fff
        </item>
        <item name="android:textColor">
            #fff
        </item>
    </style>
</resources>